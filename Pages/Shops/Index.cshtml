@page
@model Proiect_Dicui_Florin.Pages.Shops.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Shop[0].ShopName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shop[0].Director)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shop[0].Sales)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shop[0].ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shop[0].Network.NetworkName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Shop) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShopName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Director)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sales)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Network.NetworkName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
